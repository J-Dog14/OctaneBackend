export type Runner = { id: string; label: string };
export type AthleteOption = { athlete_uuid: string; name: string; email?: string | null };
export type UploadedFile = { key: string; filename: string; size: number };
